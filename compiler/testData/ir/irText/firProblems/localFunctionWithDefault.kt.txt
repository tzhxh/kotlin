fun call1(f: Function2<String, String, String>, x: String, y: String): String {
  return f.invoke(p1 = x, p2 = y)
}

fun box(): String {
  var s: String = "1"
  local fun foo(x: String, y: String = "5", z: String = "4"): String {
    return s.plus(other = x).plus(other = y).plus(other = z)
  }

  val r: String = call1(f = { // BLOCK
    local fun foo(p0: String, p1: String): String {
      return foo(x = p0, y = p1)
    }

    ::foo
  }, x = "2", y = "3")
}
